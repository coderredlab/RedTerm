v1.7.4
- Keep saved passwords inside native credential storage during SSH connections
- Bind imported SSH keys and host-key approvals to verified connection targets
- Harden terminal links, automatic responses, and high-rate SSH output handling